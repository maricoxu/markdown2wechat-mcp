# 上传失败分析和阻止发布功能

## 功能说明

修改了 `publish_wechat` 工具的逻辑，确保：
1. **先上传图片**：如果启用了 `uploadImages`，先执行图片上传
2. **检查上传结果**：验证所有图片是否上传成功
3. **成功则发布**：如果所有图片都上传成功，继续发布到微信公众号
4. **失败则分析**：如果有图片上传失败，分析失败原因并**停止发布**

## 工作流程

### 步骤 1: 图片上传

```
[发布] ========================================
[发布] 📤 步骤 1: 上传图片到 COS
[发布] ========================================
```

执行完整的 Pipeline：
- Mermaid 转换（如果启用）
- 图片收集
- COS 上传
- 链接回写

### 步骤 2: 上传结果检查

```
[发布] ========================================
[发布] 📊 上传结果统计:
[发布]   - 需要上传的图片数: 6
[发布]   - 成功上传的图片数: 6
[发布]   - 上传失败的图片数: 0
[发布] ========================================
```

### 步骤 3: 失败分析（如果有失败）

如果上传失败，会：
1. **列出失败的图片**：显示所有上传失败的图片文件名和路径
2. **分析失败原因**：根据错误代码提供诊断建议
3. **抛出错误**：停止发布流程

### 步骤 4: URL 验证（上传成功后）

即使上传成功，也会验证 URL 可访问性：
- 如果 URL 无法访问，给出警告但不阻止发布
- 提供诊断建议

### 步骤 5: 发布到微信公众号（仅在上传成功后）

只有所有图片都上传成功，才会继续发布。

## 失败分析示例

### 场景 1: 部分图片上传失败

```
[发布] ❌ 图片上传失败！共有 2 个图片上传失败
[发布] 📋 失败的图片列表:
[发布]   1. SCR-20251128-tsxp.png
[发布]      路径: /Users/.../SCR-20251128-tsxp.png
[发布]   2. SCR-20251128-ttav.png
[发布]      路径: /Users/.../SCR-20251128-ttav.png
[发布] 🔍 失败原因分析:
[发布]   错误 1: COS 上传失败: Access Denied (状态码: 403)
[发布]   💡 诊断: 权限问题
[发布]      - 检查 SecretId 和 SecretKey 是否正确
[发布]      - 检查存储桶访问权限是否允许写入
[发布]      - 检查是否配置了 IP 白名单
```

### 场景 2: 所有图片上传成功

```
[发布] ✅ 所有图片上传成功！(6/6)
[发布] 🔍 验证 COS URL 可访问性...
[发布] 📊 URL 验证结果:
[发布]   - 可访问: 6/6
[发布]   - 不可访问: 0/6
[发布] ✅ 所有 COS URL 验证通过！
[发布] ========================================
[发布] 📝 步骤 2: 发布到微信公众号
[发布] ========================================
```

## 错误类型和诊断

### 403 错误（权限问题）

**诊断建议**：
1. 检查 SecretId 和 SecretKey 是否正确
2. 检查存储桶访问权限是否允许写入
3. 检查是否配置了 IP 白名单（如果配置了，需要添加当前 IP）

### 404 错误（存储桶不存在）

**诊断建议**：
1. 检查 Bucket 名称是否正确
2. 检查 Region 是否正确

### 400 错误（参数错误）

**诊断建议**：
1. 检查 COS Key 格式是否正确
2. 检查文件大小是否超过限制

### 其他错误

**诊断建议**：
1. 网络连接问题
2. COS 服务暂时不可用
3. 文件路径或权限问题

## 使用示例

### 成功场景

```json
{
  "name": "publish_wechat",
  "arguments": {
    "filePath": "/path/to/article.md",
    "runPipeline": {
      "convertMermaid": true,
      "uploadImages": true
    }
  }
}
```

**输出**：
- 所有图片上传成功
- URL 验证通过
- 发布到微信公众号成功

### 失败场景

```json
{
  "name": "publish_wechat",
  "arguments": {
    "filePath": "/path/to/article.md",
    "runPipeline": {
      "uploadImages": true
    }
  }
}
```

**输出**：
- 部分图片上传失败
- 显示失败图片列表
- 分析失败原因
- **抛出错误，停止发布**

## 注意事项

1. **必须所有图片都上传成功**：只要有一个图片上传失败，就会停止发布
2. **URL 验证不影响发布**：即使 URL 验证失败，只要上传成功，仍会继续发布
3. **详细的错误信息**：所有错误都会提供详细的诊断建议
4. **日志完整**：每个步骤都有详细的日志输出，便于排查问题

## 改进建议

如果遇到上传失败，按照以下步骤排查：

1. **查看失败图片列表**：确认哪些图片上传失败
2. **查看错误代码**：根据错误代码（403/404/400）查看对应的诊断建议
3. **检查配置**：验证 COS 配置是否正确
4. **手动测试**：尝试手动上传一个文件，确认配置是否正确
5. **查看详细日志**：查看每个文件的上传日志，了解具体错误信息

