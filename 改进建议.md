# markdown2wechat-mcp 改进建议

基于实际使用过程中的问题和体验，总结以下改进建议：

## 🔴 高优先级问题

### 1. COS Key 路径重复问题

**问题描述**：
- 当 `keyPrefix` 已经包含日期（如 `articles/2025/11/`）时，`generateCosKey` 函数还会自动添加日期前缀
- 导致最终路径变成：`articles/2025/11/2025/11/filename.ext`（日期重复）

**当前代码**：
```typescript
// src/images/cos-uploader.ts:29-44
function generateCosKey(localPath: string, keyPrefix?: string): string {
  const datePrefix = `${year}/${month}`;
  const prefix = keyPrefix ? `${keyPrefix.replace(/\/$/, "")}/${datePrefix}` : datePrefix;
  return `${prefix}/${nameWithoutExt}${ext}`;
}
```

**改进方案**：
- **方案 A（推荐）**：检测 `keyPrefix` 是否已包含日期格式，如果包含则不再添加
- **方案 B**：添加参数控制是否自动添加日期前缀
- **方案 C**：如果 `keyPrefix` 以 `/` 结尾，则不添加日期前缀（向后兼容）

**建议实现**：
```typescript
function generateCosKey(localPath: string, keyPrefix?: string, addDatePrefix: boolean = true): string {
  const filename = basename(localPath);
  const ext = extname(filename);
  const nameWithoutExt = basename(localPath, ext);

  let prefix: string;
  if (keyPrefix) {
    const cleanPrefix = keyPrefix.replace(/\/$/, "");
    // 检测是否已包含日期格式 (YYYY/MM)
    const datePattern = /\d{4}\/\d{2}$/;
    if (addDatePrefix && !datePattern.test(cleanPrefix)) {
      const now = new Date();
      const year = now.getFullYear();
      const month = String(now.getMonth() + 1).padStart(2, "0");
      prefix = `${cleanPrefix}/${year}/${month}`;
    } else {
      prefix = cleanPrefix;
    }
  } else {
    const now = new Date();
    const year = now.getFullYear();
    const month = String(now.getMonth() + 1).padStart(2, "0");
    prefix = `${year}/${month}`;
  }
  
  return `${prefix}/${nameWithoutExt}${ext}`;
}
```

### 2. 文件存在检查逻辑问题

**问题描述**：
- 首次上传时，`headObject` 检查可能返回错误结果，导致工具误判文件已存在
- 跳过实际上传，但文件实际不存在于 COS

**当前代码**：
```typescript
// src/images/cos-uploader.ts:86-128
if (!overwrite) {
  try {
    await new Promise<void>((resolve, reject) => {
      cos.headObject(..., (err) => {
        if (!err) {
          // 文件已存在，直接返回 URL
          return { url: existingUrl };
        }
      });
    });
  } catch (error) {
    // 忽略检查错误，继续上传
  }
}
```

**改进方案**：
- 增强错误处理，明确区分 404（文件不存在）和其他错误
- 添加重试机制或更详细的日志
- 考虑添加验证步骤：上传后验证文件是否真的存在

**建议实现**：
```typescript
// 检查文件是否存在
if (!overwrite) {
  try {
    const exists = await new Promise<boolean>((resolve) => {
      cos.headObject(
        {
          Bucket: cosConfig.bucket,
          Region: cosConfig.region,
          Key: cosKey,
        },
        (err) => {
          if (!err) {
            // 文件确实存在
            resolve(true);
          } else if (err.statusCode === 404) {
            // 文件不存在，需要上传
            resolve(false);
          } else {
            // 其他错误，记录日志但继续尝试上传
            logger.warn(`[上传] 检查文件存在时出错: ${err.message}，将尝试上传`);
            resolve(false);
          }
        }
      );
    });

    if (exists) {
      // 文件已存在，返回现有 URL
      const baseUrl = cosConfig.baseUrl.replace(/\/$/, "");
      const key = cosKey.replace(/^\//, "");
      const encodedKey = key.split('/').map(segment => encodeURIComponent(segment)).join('/');
      const existingUrl = `${baseUrl}/${encodedKey}`;
      
      logger.info(`[上传] 文件已存在，跳过上传: ${basename(localPath)}`);
      logger.info(`[上传] 使用现有 URL: ${existingUrl}`);
      
      return {
        localPath,
        cosKey,
        url: existingUrl,
      };
    }
  } catch (error: any) {
    logger.warn(`[上传] 检查文件存在时出错: ${error.message}，将尝试上传`);
  }
}
```

### 3. 封面图片（Cover）自动上传支持

**问题描述**：
- Frontmatter 中的 `cover` 字段使用本地路径时，不会被自动上传到 COS
- 发布到微信时，封面图片仍然是本地路径，无法正常显示

**当前行为**：
- `collectLocalImages` 只收集 Markdown 正文中的图片（`![alt](path)` 格式）
- 不处理 Frontmatter 中的 `cover` 字段

**改进方案**：
- 扩展 `collectLocalImages` 函数，同时收集 Frontmatter 中的 `cover` 图片
- 上传后，同时更新 Frontmatter 中的 `cover` 路径为 COS URL

**建议实现**：
```typescript
// src/images/collect.ts
export interface ImageInfo {
  alt?: string;
  localPath: string;
  originalUrl: string;
  type?: 'markdown' | 'cover'; // 添加类型标识
}

// 收集图片时，同时处理 Frontmatter
export function collectLocalImages(content: string, markdownFilePath: string): ImageInfo[] {
  const images: ImageInfo[] = [];
  const markdownDir = dirname(markdownFilePath);
  
  // 1. 收集 Markdown 正文中的图片
  const imageRegex = /!\[([^\]]*)\]\(([^)]+)\)/g;
  // ... 现有逻辑 ...
  
  // 2. 收集 Frontmatter 中的 cover 图片
  const frontmatterMatch = content.match(/^---\s*\n([\s\S]*?)\n---/);
  if (frontmatterMatch) {
    const frontmatter = frontmatterMatch[1];
    const coverMatch = frontmatter.match(/^cover:\s*(.+)$/m);
    if (coverMatch) {
      const coverPath = coverMatch[1].trim();
      // 跳过网络图片
      if (!coverPath.startsWith("http://") && !coverPath.startsWith("https://")) {
        let absolutePath: string;
        if (isAbsolute(coverPath)) {
          absolutePath = coverPath;
        } else {
          absolutePath = resolve(markdownDir, coverPath);
        }
        
        if (existsSync(absolutePath)) {
          images.push({
            alt: "cover",
            localPath: absolutePath,
            originalUrl: coverPath,
            type: 'cover',
          });
        }
      }
    }
  }
  
  return images;
}
```

**同时需要更新 `rewrite-links.ts`**：
```typescript
// 在替换图片链接时，同时更新 Frontmatter 中的 cover
export function rewriteImageLinks(
  content: string,
  markdownFilePath: string,
  uploadResults: CosUploadResult[]
): string {
  // ... 现有逻辑替换 Markdown 中的图片 ...
  
  // 更新 Frontmatter 中的 cover
  const frontmatterMatch = content.match(/^(---\s*\n)([\s\S]*?)(\n---)/);
  if (frontmatterMatch) {
    let frontmatter = frontmatterMatch[2];
    uploadResults.forEach(result => {
      // 查找对应的 cover 图片
      const coverMatch = frontmatter.match(/^cover:\s*(.+)$/m);
      if (coverMatch && result.localPath === resolve(dirname(markdownFilePath), coverMatch[1].trim())) {
        frontmatter = frontmatter.replace(
          /^cover:\s*(.+)$/m,
          `cover: ${result.url}`
        );
      }
    });
    content = content.replace(
      /^(---\s*\n)([\s\S]*?)(\n---)/,
      `$1${frontmatter}$3`
    );
  }
  
  return content;
}
```

## 🟡 中优先级问题

### 4. 错误提示优化

**问题描述**：
- IP 白名单错误提示不够友好
- 缺少配置验证和错误提示

**改进方案**：
- 在启动时验证配置完整性
- 提供更友好的错误提示和解决建议
- 添加配置检查工具

**建议实现**：
```typescript
// src/config/load.ts
export function validateConfig(): { valid: boolean; errors: string[] } {
  const errors: string[] = [];
  const config = getConfig();
  
  // 检查微信配置
  if (!config.wechat?.appId || !config.wechat?.appSecret) {
    errors.push("微信配置不完整：缺少 WECHAT_APP_ID 或 WECHAT_APP_SECRET");
  }
  
  // 检查 COS 配置（如果使用图片上传功能）
  // 注意：COS 配置是可选的，只有在使用上传功能时才需要
  
  return {
    valid: errors.length === 0,
    errors,
  };
}
```

### 5. 上传进度和状态反馈

**问题描述**：
- 批量上传大量图片时，缺少进度反馈
- 无法知道上传是否成功

**改进方案**：
- 添加上传进度回调
- 提供更详细的上传状态信息
- 支持断点续传（可选）

### 6. 路径处理优化

**问题描述**：
- 相对路径和绝对路径的处理可能不够健壮
- 特殊字符和中文路径的处理

**改进方案**：
- 统一路径规范化处理
- 增强对特殊字符的支持
- 添加路径验证

## 🟢 低优先级优化

### 7. 配置管理优化

**改进建议**：
- 支持配置文件（config.json）和环境变量混合使用
- 提供配置验证工具
- 支持配置模板生成

### 8. 日志和调试

**改进建议**：
- 提供更详细的日志级别控制
- 支持日志输出到文件
- 添加调试模式

### 9. 文档完善

**改进建议**：
- 添加常见问题 FAQ
- 提供更多使用示例
- 添加故障排查指南

### 10. 测试覆盖

**改进建议**：
- 增加单元测试覆盖
- 添加集成测试
- 提供测试工具和示例

## 📋 实施优先级建议

1. **立即修复**：
   - ✅ COS Key 路径重复问题（影响用户体验）
   - ✅ 文件存在检查逻辑问题（导致上传失败）

2. **近期优化**：
   - 封面图片自动上传支持
   - 错误提示优化

3. **长期改进**：
   - 上传进度反馈
   - 配置管理优化
   - 文档完善

## 🔧 快速修复建议

对于当前最紧急的问题（路径重复），可以快速修复：

```typescript
// 快速修复：检测 keyPrefix 是否已包含日期
function generateCosKey(localPath: string, keyPrefix?: string): string {
  const filename = basename(localPath);
  const ext = extname(filename);
  const nameWithoutExt = basename(localPath, ext);

  let prefix: string;
  if (keyPrefix) {
    const cleanPrefix = keyPrefix.replace(/\/$/, "");
    // 简单检测：如果 keyPrefix 以 YYYY/MM 格式结尾，则不添加日期
    const datePattern = /\/\d{4}\/\d{2}$/;
    if (datePattern.test(cleanPrefix)) {
      prefix = cleanPrefix;
    } else {
      const now = new Date();
      const year = now.getFullYear();
      const month = String(now.getMonth() + 1).padStart(2, "0");
      prefix = `${cleanPrefix}/${year}/${month}`;
    }
  } else {
    const now = new Date();
    const year = now.getFullYear();
    const month = String(now.getMonth() + 1).padStart(2, "0");
    prefix = `${year}/${month}`;
  }
  
  return `${prefix}/${nameWithoutExt}${ext}`;
}
```

## 📝 总结

本次使用过程中发现的主要问题：
1. ✅ **路径重复**：已识别，需要修复
2. ✅ **文件检查逻辑**：已识别，需要优化
3. ✅ **封面图片处理**：已识别，需要支持
4. ✅ **错误提示**：可以优化

这些问题大部分都是可以快速修复的，建议优先处理前两个高优先级问题。

