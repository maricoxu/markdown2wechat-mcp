# 腾讯云 COS 申请与配置指南

本文档详细说明如何申请腾讯云对象存储（COS）服务，并配置到本项目中。

## 📋 目录

1. [为什么要使用 COS](#为什么要使用-cos)
2. [申请腾讯云账号](#申请腾讯云账号)
3. [开通 COS 服务](#开通-cos-服务)
4. [创建存储桶](#创建存储桶)
5. [获取访问密钥](#获取访问密钥)
6. [配置到项目](#配置到项目)
7. [验证配置](#验证配置)
8. [费用说明](#费用说明)
9. [常见问题](#常见问题)

---

## 为什么要使用 COS

### 使用场景

微信公众号发布时，需要所有图片都是**外部可访问的 URL**，不能使用本地路径。COS 的作用是：

1. **上传本地图片** → 转换为可访问的 URL
2. **统一管理** → 所有图片集中存储
3. **CDN 加速** → 腾讯云提供全球 CDN 加速
4. **成本可控** → 按使用量付费，价格低廉

### 不使用 COS 的情况

如果你的图片**已经是外部 URL**（如 GitHub、CDN、图床等），可以跳过 COS 配置。

---

## 申请腾讯云账号

### 步骤 1: 注册账号

1. 访问 [腾讯云官网](https://cloud.tencent.com/)
2. 点击右上角 **"免费注册"**
3. 填写账号信息（手机/邮箱）
4. 完成实名认证（**必须**，用于开通服务）

> **注意**：实名认证需要身份证或企业信息，建议提前准备。

### 步骤 2: 登录控制台

1. 注册完成后，访问 [腾讯云控制台](https://console.cloud.tencent.com/)
2. 使用注册的账号登录

---

## 开通 COS 服务

### 步骤 1: 进入 COS 控制台

1. 登录腾讯云控制台
2. 在顶部搜索框输入 **"对象存储"** 或 **"COS"**
3. 点击进入 **"对象存储 COS"** 服务

### 步骤 2: 开通服务（首次使用）

1. 如果是首次使用，会看到 **"立即开通"** 按钮
2. 点击开通（**免费**，无费用）
3. 阅读并同意服务协议
4. 开通成功后，自动跳转到 COS 控制台

---

## 创建存储桶

### 什么是存储桶（Bucket）？

存储桶类似于文件夹，用于存放你的图片文件。

### 步骤 1: 创建存储桶

1. 在 COS 控制台，点击左侧 **"存储桶列表"**
2. 点击 **"创建存储桶"** 按钮

### 步骤 2: 配置存储桶

填写以下信息：

**基础配置**:
- **名称**: 填写一个**唯一**的存储桶名称（如：`my-blog-images-2024`）
  - ⚠️ 名称规则：
    - 只能包含小写字母、数字、中划线和下划线
    - 长度 3-63 个字符
    - 全局唯一（不能与其他用户重复）
- **所属地域**: 选择离你最近的地域（如：`ap-guangzhou` 广州）
  - 💡 **推荐**：
    - 中国大陆：`ap-guangzhou`（广州）
    - 海外：`ap-singapore`（新加坡）

**访问权限**（重要）:
- **公有读私有写** ✅ （推荐）
  - 图片可以被外部访问（公众号需要）
  - 只有你（通过密钥）可以上传
- **私有读写** ⚠️
  - 需要签名才能访问，不适合公众号使用

**其他配置**:
- **存储类型**: `标准存储`（默认，适合频繁访问）
- **服务端加密**: `不加密`（默认即可）
- **日志记录**: 可选，用于追踪访问

### 步骤 3: 完成创建

点击 **"创建"** 按钮，等待创建完成（通常几秒钟）。

---

## 获取访问密钥

访问密钥用于程序调用 COS API 上传图片。

### 步骤 1: 进入 API 密钥管理

1. 点击控制台右上角 **头像/用户名**
2. 在下拉菜单中选择 **"访问管理"** 或 **"API密钥管理"**
3. 或者直接访问：https://console.cloud.tencent.com/cam/capi

### 步骤 2: 创建密钥

1. 在 **"API密钥管理"** 页面，点击 **"新建密钥"**
2. 输入密钥名称（如：`wenyan-mcp`）
3. 点击 **"确定"**

### 步骤 3: 保存密钥信息

⚠️ **重要**：密钥只显示一次，请立即保存！

你会得到两个关键信息：
- **SecretId**: 类似 `YOUR_SECRET_ID_HERE`（示例格式）
- **SecretKey**: 类似 `YOUR_SECRET_KEY_HERE`（示例格式）

> **安全提示**：
> - 不要将密钥提交到 Git 仓库
> - 不要分享给他人
> - 建议创建子账号，使用最小权限原则

---

## 配置到项目

### 方法一：使用 `.env` 文件（推荐）

在项目根目录创建或编辑 `.env` 文件：

```env
# 腾讯云 COS 配置
COS_SECRET_ID=YOUR_SECRET_ID_HERE
COS_SECRET_KEY=YOUR_SECRET_KEY_HERE
COS_REGION=ap-guangzhou
COS_BUCKET=my-blog-images-2024
```

**配置说明**:
- `COS_SECRET_ID`: 你的 SecretId
- `COS_SECRET_KEY`: 你的 SecretKey
- `COS_REGION`: 存储桶所属地域（如：`ap-guangzhou`）
- `COS_BUCKET`: 存储桶名称（如：`my-blog-images-2024`）

### 方法二：使用环境变量

```bash
export COS_SECRET_ID=YOUR_SECRET_ID_HERE
export COS_SECRET_KEY=YOUR_SECRET_KEY_HERE
export COS_REGION=ap-guangzhou
export COS_BUCKET=my-blog-images-2024
```

### 方法三：MCP 配置文件中设置

在你的 MCP 客户端配置文件中：

```json
{
  "mcpServers": {
    "wenyan-mcp": {
      "name": "公众号助手",
      "command": "wenyan-mcp",
      "env": {
        "WECHAT_APP_ID": "your_app_id",
        "WECHAT_APP_SECRET": "your_app_secret",
        "COS_SECRET_ID": "your_cos_secret_id",
        "COS_SECRET_KEY": "your_cos_secret_key",
        "COS_REGION": "ap-guangzhou",
        "COS_BUCKET": "your_bucket_name"
      }
    }
  }
}
```

---

## 验证配置

### 测试上传功能

使用 `image_upload_cos` 工具测试：

```json
{
  "tool": "image_upload_cos",
  "arguments": {
    "filePath": "/path/to/your/article.md"
  }
}
```

### 手动验证

1. **检查 `.env` 文件**:
   ```bash
   cat .env | grep COS
   ```

2. **查看上传后的 URL**:
   - 上传成功后，Markdown 中的图片路径会被替换为 COS URL
   - 格式类似：`https://your-bucket-1234567890.cos.ap-guangzhou.myqcloud.com/images/xxx.png`

3. **在浏览器中打开 URL**:
   - 如果能正常显示图片，说明配置成功 ✅

---

## 费用说明

### 免费额度

腾讯云 COS 提供**免费额度**（新用户）：

1. **存储容量**: 50GB/月（免费）
2. **流量**: 10GB/月（免费）
3. **请求次数**: 100万次/月（免费）

> **注意**：免费额度通常足够个人博客使用。

### 超出免费额度后的费用

| 项目 | 价格（示例：广州地域） |
|------|---------------------|
| 存储 | 约 ¥0.118/GB/月 |
| 外网下行流量 | 约 ¥0.50/GB |
| PUT 请求 | 约 ¥0.01/万次 |

> **实际费用**：
> - 个人博客：通常 **0 元**（在免费额度内）
> - 中型网站：约 **¥5-20/月**

### 查看费用

1. 登录腾讯云控制台
2. 进入 **"费用中心"**
3. 查看 **"账单概览"**

---

## 常见问题

### Q1: 存储桶名称被占用怎么办？

**解决方案**:
- 尝试添加数字或日期后缀
- 例如：`my-images-2024-11-01`
- 或者：`blog-images-abc123`

### Q2: 如何找到存储桶的地域代码？

在 COS 控制台的存储桶列表中，可以看到每个存储桶的**所属地域**，常见代码：

| 地域 | 地域代码 |
|------|---------|
| 广州 | `ap-guangzhou` |
| 北京 | `ap-beijing` |
| 上海 | `ap-shanghai` |
| 成都 | `ap-chengdu` |
| 重庆 | `ap-chongqing` |
| 新加坡 | `ap-singapore` |
| 香港 | `ap-hongkong` |

### Q3: 密钥忘记了怎么办？

**解决方案**:
1. 登录 [API密钥管理](https://console.cloud.tencent.com/cam/capi)
2. 找到对应的密钥
3. 点击 **"禁用"** 后重新创建新密钥
4. ⚠️ 注意：禁用旧密钥后，使用旧密钥的程序将无法工作

### Q4: 图片上传失败，提示权限错误？

**可能原因**:
1. SecretId 或 SecretKey 错误
2. 存储桶名称或地域不匹配
3. 存储桶访问权限设置为"私有读写"

**解决方案**:
1. 检查 `.env` 文件中的配置是否正确
2. 确认存储桶访问权限为 **"公有读私有写"**
3. 检查 SecretId/SecretKey 是否有权限访问该存储桶

### Q5: 如何查看已上传的图片？

**方法**:
1. 登录 COS 控制台
2. 进入对应的存储桶
3. 在 **"文件列表"** 中查看所有已上传的文件

### Q6: 如何删除不需要的图片？

**方法**:
1. 在 COS 控制台的文件列表中
2. 选中要删除的文件
3. 点击 **"删除"** 按钮

> **注意**：删除后无法恢复，请谨慎操作。

### Q7: 可以限制上传的文件类型吗？

**可以**，但需要在代码层面实现。当前版本支持所有图片格式（PNG、JPG、GIF、SVG 等）。

### Q8: 上传的图片会自动生成 CDN 加速吗？

**默认不会**。如果需要 CDN 加速：
1. 在 COS 控制台开启 **"CDN 加速"**
2. 绑定自定义域名
3. 配置 CDN 回源

---

## 完整配置示例

### `.env` 文件示例

```env
# 微信公众号配置
WECHAT_APP_ID=wx1234567890abcdef
WECHAT_APP_SECRET=abc123def456ghi789jkl012mno345pq

# 腾讯云 COS 配置
COS_SECRET_ID=YOUR_SECRET_ID_HERE
COS_SECRET_KEY=YOUR_SECRET_KEY_HERE
COS_REGION=ap-guangzhou
COS_BUCKET=my-blog-images-2024

# Mermaid 配置（可选）
MERMAID_ENGINE=local
MERMAID_SCALE=1.5
MERMAID_BACKGROUND=#ffffff
MERMAID_FORMAT=png
OUTPUT_DIR=.assets
```

---

## 安全建议

1. **不要提交 `.env` 到 Git**:
   ```bash
   # 确保 .env 在 .gitignore 中
   echo ".env" >> .gitignore
   ```

2. **使用子账号和最小权限**:
   - 创建子账号，只授予 COS 上传权限
   - 不使用主账号密钥

3. **定期轮换密钥**:
   - 建议每 3-6 个月更换一次密钥

4. **监控使用情况**:
   - 在腾讯云控制台开启 **"访问日志"**
   - 定期检查异常访问

---

## 下一步

配置完成后，你可以：

1. **测试图片上传**:
   ```json
   {
     "tool": "image_upload_cos",
     "arguments": {
       "filePath": "/path/to/article.md"
     }
   }
   ```

2. **使用完整 Pipeline**:
   ```json
   {
     "tool": "publish_wechat",
     "arguments": {
       "filePath": "/path/to/article.md",
       "runPipeline": {
         "convertMermaid": true,
         "uploadImages": true
       }
     }
   }
   ```

---

## 相关文档

- [腾讯云 COS 官方文档](https://cloud.tencent.com/document/product/436)
- [COS 控制台](https://console.cloud.tencent.com/cos)
- [API 密钥管理](https://console.cloud.tencent.com/cam/capi)
- [项目 README](../README.md)
- [图片上传功能文档](./README.md#3-图片上传到-cos-image_upload_cos)

---

## 技术支持

如果遇到问题：

1. 检查配置是否正确
2. 查看项目日志输出
3. 参考 [腾讯云 COS 官方文档](https://cloud.tencent.com/document/product/436)
4. 提交 Issue 到项目仓库

祝使用愉快！🎉

