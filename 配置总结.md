# 配置总结

## 完整配置示例

### MCP 配置文件（推荐方式）

在 `~/.cursor/mcp.json` 或你的 MCP 客户端配置文件中：

```json
{
  "mcpServers": {
    "wenyan-mcp": {
      "name": "公众号助手",
      "command": "node",
      "args": [
        "/path/to/markdown2wechat-mcp/dist/index.js"
      ],
      "disabled": false,
      "autoApprove": true,
      "description": "📝 文颜 MCP - Fork版本，支持Markdown文章发布到微信公众号",
      "env": {
        "WECHAT_APP_ID": "your_wechat_app_id",
        "WECHAT_APP_SECRET": "your_wechat_app_secret",
        "COS_SECRET_ID": "your_cos_secret_id",
        "COS_SECRET_KEY": "your_cos_secret_key",
        "COS_REGION": "ap-shanghai",
        "COS_BUCKET": "your_bucket_name",
        "COS_BASE_URL": "https://your-bucket-name-APPID.cos.region.myqcloud.com"
      }
    }
  }
}
```

### 环境变量说明

#### 微信公众号配置（必需）

| 环境变量 | 说明 | 获取方式 |
|---------|------|---------|
| `WECHAT_APP_ID` | 微信公众号平台的 App ID | [微信公众平台](https://mp.weixin.qq.com/) → "开发" → "基本配置" |
| `WECHAT_APP_SECRET` | 微信平台的 App Secret | 同上 |

#### 腾讯云 COS 配置（图片上传功能需要）

| 环境变量 | 说明 | 获取方式 |
|---------|------|---------|
| `COS_SECRET_ID` | 腾讯云 API 密钥 SecretId | [腾讯云 API 密钥管理](https://console.cloud.tencent.com/cam/capi) |
| `COS_SECRET_KEY` | 腾讯云 API 密钥 SecretKey | 同上 |
| `COS_REGION` | 存储桶所属地域 | COS 控制台 → 存储桶 → 概览（如：`ap-shanghai`、`ap-guangzhou`） |
| `COS_BUCKET` | 存储桶名称 | COS 控制台 → 存储桶列表 |
| `COS_BASE_URL` | 存储桶访问地址 | COS 控制台 → 存储桶 → 概览 → 访问域名（格式：`https://bucket-name-APPID.cos.region.myqcloud.com`） |

### 配置优先级

1. **MCP 配置文件中的 `env`**（最高优先级）
2. **项目根目录的 `.env` 文件**
3. **系统环境变量**

### 配置验证

配置完成后，重启 MCP 服务器，然后使用 `publish_wechat` 工具测试。如果配置正确，应该能够：

1. ✅ 成功上传图片到 COS
2. ✅ 生成正确的 COS URL
3. ✅ 成功发布到微信公众号

### 常见问题

#### Q: 如何确认配置是否正确？

**方法 1：查看日志**
- 使用 `publish_wechat` 工具时，会输出详细的上传日志
- 如果配置错误，会在日志中显示具体的错误信息

**方法 2：手动测试**
- 尝试上传一个简单的图片
- 检查生成的 COS URL 是否可以访问

#### Q: 配置后需要重启吗？

**是的**，修改配置后需要：
1. 重启 MCP 服务器（在 Cursor 中重新加载或重启 Cursor）
2. 确保新配置生效

#### Q: COS 配置是可选的吗？

**部分可选**：
- 如果**不使用图片上传功能**，可以不配置 COS
- 如果**使用图片上传功能**（`uploadImages: true`），必须配置所有 COS 环境变量

#### Q: 如何获取 COS 配置信息？

参考详细配置指南：
- [COS 配置步骤说明](COS配置步骤说明.md)
- [COS 配置指南](docs/COS_SETUP_GUIDE.md)

### 安全建议

1. **不要将配置文件提交到 Git**
   - 确保 `.env` 在 `.gitignore` 中
   - MCP 配置文件通常不在 Git 仓库中

2. **使用最小权限原则**
   - 创建子账号，只授予必要的权限
   - 不使用主账号密钥

3. **定期轮换密钥**
   - 建议每 3-6 个月更换一次密钥

