# 使用说明：重启 MCP 服务器

## 重要提示

**代码已更新并编译完成，但需要重启 MCP 服务器才能生效！**

## 重启步骤

### 方法 1: 在 Cursor 中重启 MCP 服务器

1. 打开 Cursor 设置
2. 找到 MCP 服务器配置
3. 重启 `wenyan-mcp` 服务器

### 方法 2: 重启 Cursor

1. 完全退出 Cursor
2. 重新启动 Cursor
3. MCP 服务器会自动重新加载

## 新功能说明

重启后，`publish_wechat` 工具将具备以下新功能：

### 1. 详细的上传日志
- 每个文件的上传过程都有详细日志
- 包括文件信息、COS 配置、上传参数、URL 生成等

### 2. 上传结果检查
- 统计需要上传、成功上传、失败的图片数量
- 如果有失败，会列出失败的图片并分析原因

### 3. URL 验证和阻止发布
- 上传成功后，自动验证所有 COS URL 可访问性
- 如果 URL 无法访问，会：
  - 列出所有无法访问的 URL
  - 提供详细的诊断建议
  - **抛出错误，阻止发布**

### 4. 失败原因分析
根据不同错误类型提供诊断建议：
- 403 错误：权限问题
- 404 错误：存储桶不存在
- 400 错误：参数错误
- URL 无法访问：存储桶权限、URL 生成、网络问题等

## 使用示例

重启后，使用以下命令发布文章：

```json
{
  "name": "publish_wechat",
  "arguments": {
    "filePath": "/path/to/article.md",
    "runPipeline": {
      "convertMermaid": true,
      "uploadImages": true
    }
  }
}
```

## 预期输出

### 成功场景

```
[发布] ========================================
[发布] 📤 步骤 1: 上传图片到 COS
[发布] ========================================
[上传] ========================================
[上传] 📁 文件信息: ...
[上传] ✅ 上传成功！
[上传] ✅ URL 验证通过！
[发布] ✅ 所有图片上传成功！(6/6)
[发布] 🔍 验证 COS URL 可访问性...
[发布] ✅ 所有 COS URL 验证通过！
[发布] ========================================
[发布] 📝 步骤 2: 发布到微信公众号
[发布] ========================================
```

### 失败场景

```
[发布] ❌ 图片上传失败！共有 2 个图片上传失败
[发布] 📋 失败的图片列表: ...
[发布] 🔍 失败原因分析: ...
→ 抛出错误，停止发布
```

或

```
[发布] ✅ 所有图片上传成功！(6/6)
[发布] 🔍 验证 COS URL 可访问性...
[发布] ❌ 以下 COS URL 无法访问: ...
[发布] 🔍 失败原因分析: ...
→ 抛出错误，停止发布
```

## 注意事项

1. **必须重启 MCP 服务器**：新代码需要重启才能生效
2. **查看详细日志**：所有诊断信息都在日志中
3. **按步骤排查**：根据诊断建议逐步排查问题
4. **检查 COS 配置**：确保存储桶访问权限设置为"公有读私有写"

## 故障排查

如果重启后仍然遇到问题：

1. **检查编译是否成功**：运行 `npm run build`
2. **检查 MCP 服务器是否重启**：查看 Cursor 的 MCP 服务器状态
3. **查看详细日志**：所有诊断信息都在日志输出中
4. **手动测试 COS URL**：在浏览器中访问 URL 确认文件是否存在

